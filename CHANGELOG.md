# Changelog

## 0.1.7-rc.4 — 2026-08-08

新增「重发最近一条」快捷键与历史记录单条删除，并修复历史页更新不及时。

- **历史记录页即时刷新**：会话终态落账（发送成功/失败/取消）后立即推送
  `kotone://history-updated` 事件，停留历史页时新记录自动出现，无需切页再切回
  （此前仅页面挂载时读取一次）。
- **历史记录页支持删除单条**：每条记录右侧新增删除按钮（✕），删除该条
  记录；记录带录音且不再被其他记录引用时（error→重试会共享同 sessionId
  音频）一并删除对应 wav，其余记录与音频不受影响。CLI 同步新增
  `kotone-cli log delete --session-id <id> --ts <ts>` 子命令（参数取自
  `log list --json`，记录不存在时幂等成功）。
- 「高级」页新增**重发最近一条热键**（默认关闭，可点击录入；如 Alt+F6）：
  空闲时按下，把历史记录中最新一条发送成功的文本按当前游戏适配与频道重新
  注入到前台窗口（与普通发送同一路径，含焦点恢复、注入超时与失败重试）。
  会话进行中（正在说话/发送/预览）按下静默忽略，不打断当前输入；历史中
  无发送成功的文本时无效果。与录制热键/频道切换热键冲突时拒绝注册并提示。
  热键经 LL 钩子与 RegisterHotKey 两个后端、桌面壳与 CLI 同时生效。
- 修复独奏模式结束时会留下一条空取消记录的问题。

- 取消会话时若没有任何语音产出（STT 从未出过识别 partial，如结束独奏
  模式时刚开出的空段、或没开口就取消），不再写入历史记录——此前每次
  结束独奏都会在历史里多一条「（未说完成取消）」。说了一半才取消的
  cancelled 记录照常保留（docs/cli.md 已同步说明）。

## 0.1.7-rc.3 — 2026-08-06

精简首页提示，模型缺失报错自动引导处理。

- 移除通用页「没有可用的识别模型」横幅：未下载模型时点击启动的报错
  本身就会提醒处理，无需页面上方再占一处常驻提示。
- 启动报错中出现模型缺失 / 文件不齐备时，现在会自动跳转到「高级」页
  模型管理，可就地下载补全；此前仅部分报错能触发跳转。
- 关于页版本号改为构建时注入，与安装包版本保持一致，不再依赖页面内
  兜底常量。
- 新增版本提升脚本 `pnpm release:bump`：一次调用同步
  package.json / tauri.conf.json / Cargo.toml 三处版本并预置 CHANGELOG
  小节，发布流程见 docs/release-checklist.md。

## 0.1.7-rc.2 — 2026-08-05

发布候选第二版：游戏配置整包导入导出，VAD 与热词权重可配置。

- 游戏适配支持**整包导入/导出/删除**：导出 `.zip` 配置包（含
  profile.json + 图标），可分享或"导出 → 手工改成别的游戏 → 导入"；
  导入自动生成新 id 用包名区分，删除内置配置 = 恢复出厂、删除导入的 =
  永久移除。
- profile 图标数据化：LOL 图标移入核心资源随本体分发，自定义 profile
  导入时带自己的图标；老配置升级自动补齐 icon 字段。
- 高级页新增「VAD 高级设置」与「热词权重」：silero VAD 判定阈值 /
  最短语音 / 最短静音，以及热词命中加分（原硬编码 3.5）均可配置，
  用于降低背景噪声被误判成语音、或被识别成热词（如独奏模式句首「DPS」）
  的问题。VAD 参数下一条会话立即生效；热词权重需「停止 → 启动」重建
  recognizer 后生效。

## 0.1.7-rc.1 — 2026-08-04

面向用户反馈的发布候选版本，重点修复模拟输入卡死，并完善模型、录音与存储管理。

- 为编排器和手动发送命令增加 10 秒输入注入超时；安全软件或反作弊挂起
  `SendInput` 时界面不再永久卡死，错误态保留文本并允许重试。
- 区分 UIPI 权限不足与安全软件拦截，分别给出管理员运行与安全软件排查提示。
- 首次向导支持在下载前选择模型目录；高级页增加模型与历史记录目录管理，
  主页在模型缺失时提供直达下载入口。
- 模型下载增加磁盘空间预检、取消、断点续传保留与并发互斥。
- 模型目录迁移在引擎运行时主动拒绝；同名文件按内容校验，冲突文件改名保留，
  避免同大小但内容不同的文件被误删。
- 录音设备中途故障会中止当前会话并提示，不再永久停留在 Listening。
- 首次向导允许用户跳过输入环境拦截探针，并在录入热键后展示常见键位冲突提示。

## 0.1.6 — 2026-07-31

模拟输入被系统拦截的提示与排查引导，修复注入失败后热键失能。

- 新增独立的键盘输入环境自检服务：首次引导进入第三步时主动验证
  `WH_KEYBOARD_LL` 与 `SendInput`，无需用户先点击录入或启动运行时；明确检测到
  系统拒绝模拟输入时，提前引导将 Kotone 加入 360、火绒等安全软件信任区，
  处理后可就地重新检测。
- 修复合成 F24 探测事件未回环时误报安全软件拦截的问题：`SendInput` 已完整接受
  但 Hook 未观察到的结果改为不确定态，由后续真实按键兜底验证。
- 修复 LL Hook 刷新后的就绪竞态：安装线程现在先创建消息队列并进入可泵送状态，
  再通知录入流程继续；窗口内收到按键不再直接归因于 360，而是安全完成兜底录入
  并留下诊断日志。
- 修复对讲机模式「说完立刻松手」稳定丢最后 1-2 字的问题：松手不再立即
  停止采集，而是进入 500ms 宽限期继续收音、倒计时到点才转写发送——此前
  松手即停会切掉仍在传输链路上的句尾音频与「人边说边松」时还在空气中的
  尾音（真机实验实锤：1824ms 音频只进入 1600ms）；宽限期内再次按下热键
  可收回结束、继续说这一句。录音笔 / 一句话 / 独奏模式结束逻辑不变。
- 热键匹配器在「主键按下但修饰键不匹配」时上报 MainKeyMissed 诊断事件，
  日志可区分「钩子存活但修饰键状态卡住」与「钩子被系统摘除」两类故障；
  同时修饰键状态改为每个按键事件前从物理状态（GetAsyncKeyState）同步
  （我们从不吞修饰键，物理状态恒准确），自愈一切「修饰键 up 丢失」
  （安全桌面 / UAC / 钩链异常）造成的主键失配放行。
- 移除 webview 热键兜底通道（localHotkeyBridge / trigger_local_hotkey）：
  该通道在 Kotone 窗口聚焦时接管按键，而 CapsLock 等锁定键在 WebView2
  物理松开不产生 DOM keyup，会掩盖钩子侧的真实故障并引入新的卡死路径；
  移除后热键统一由 LL 钩子 / RegisterHotKey 处理，故障直接可见、可诊断。
- 引导流程精简为 3 步：第 4 步「发送测试」暂缓（该步骤在 Kotone 窗口聚焦时
  依赖的热键路径在部分环境下不稳定，问题隔离后再恢复），第三步「完成」
  保存配置直接进入主页，不再自动启动引擎。
- 开发调试：`pnpm tauri dev -- --console` 启动后自动打开 webview 控制台
  （WebView2 无右键检查入口）。
- 修复开发环境 overlay 窗口的 CSP 噪音：connect-src 放行 Tauri IPC 自定义
  协议（http://ipc.localhost），不再刷 CSP violation 报错。

- 新增「发送被系统拦截」排查引导弹窗：SendInput 0/N 全部未落地（安全软件
  主动防御 / 游戏反作弊 / 远控软件在系统层拦截模拟输入）时，明确告知原因并
  给出按概率排序的排查清单与记事本快速验证路径，引导页注入测试可就地重试。
- 修复注入失败后热键完全失能、只能重启 runtime 的问题：对讲机模式下
  发送失败（保留文本的 Error 态）按下热键现在会直接重试发送。
- 修复错误提示显示为「[object Object]」的问题：结构化注入错误（含真实
  原因，如「SendInput 被系统拦截（0/10 成功）」）现在正确透出到界面。
- 注入失败在诊断事件与历史记录中统一使用错误码 INJECTION_BLOCKED /
  INJECTION_ELEVATION_REQUIRED / INJECTION_FAILED，修复此前 events.csv 与
  history-metadata 错误码不一致的问题。
- 模型就绪检查失败时记录具体缺失/大小不符的文件并短暂重试，修复运行中
  停止/启动后偶发误报「模型未下载」的问题。

## 0.1.5 — 2026-07-30

管理员提权链路修复与提示流程重构。

- 修复勾选「启动时自动请求管理员权限」后双击启动无反应的问题：自动提权判定前移到
  Tauri/单实例插件初始化之前，未提权父进程不再持有单实例锁，提权子进程不再被误判
  为「第二实例」转发后自杀（此前只能右键「以管理员身份运行」打开）。
- 开场提权提示拆为两段式：先询问「是否以管理员身份重启」，仅保留「不再提示」勾选项，
  勾选并跳过后提示可随时在「高级」页面以管理员身份重启；重启成功后再单独询问
  「是否启动时自动请求管理员权限」，同样支持「不再提醒」并保留「高级」页开关。
- 新增 autoAdminPromptDismissed 设置项，两段提示独立记忆用户表态；「高级」页开启
  自动提权开关时同步记录两段提示均已表态。


聊天频道切换（ADR-008）与界面视觉升级。

- 游戏适配 profile 新增聊天频道声明：按键策略（专属开框键）与前缀策略（如 /all）
  正交可组合；英雄联盟内置「队伍 / 所有人」双频道，存量配置自动补齐。
- 新增「频道切换热键」（默认 Shift+CapsLock，「高级」页可改），按声明顺序循环
  切换频道；悬浮窗非默认频道常驻徽标，切换瞬间短暂露出并弹跳提示。
- 录制热键与频道切换热键冲突双向校验：保存时前端预检拒绝，后端注册时再次兜底
  并在「高级」页显示错误。
- 「游戏适配」页展示每个 profile 支持的频道与切换键。
- Toast 提示带上人物贴纸（成功/信息/警告/错误各一张）；首页欢迎面板新增游戏
  弹幕文字飘过动画（跟随系统减弱动态效果设置）。
- 「高级」页去掉收起/展开摘要块与「引擎」相关表述（产品只内置一套识别方案），
  新增「语言」设置（当前仅中文，为多语言埋伏笔）。
- 标题栏品牌区宽度与侧边栏对齐。
- 修复「关于」页 GitHub 链接点击无反应的问题：改由系统默认浏览器打开。
- 修复跳过首次引导后交互模式为空的问题：默认模式定为「对讲机」，老配置
  启动时自动迁移补齐；跳过向导也会明确写入所选模式。
- 「高级」页只恒显 X-ASR 识别模型，SenseVoice / FunASR 收进「备用」折叠区。
- 安装/卸载在检测运行中进程时增加 tasklist 兜底：以管理员身份运行的实例
  也能被识别，提示先退出托盘进程，不再出现无法写入 kotone.exe 的原始报错。
- 检查更新增加多镜像回退通道：依次尝试 ghfast.top / gh-proxy.com / ghproxy.net /
  gh.llkk.cc 四个 GitHub 镜像（各配独立的安装包地址，单镜像故障自动落到下一个），
  全部失败后回退 GitHub 直连。

## 0.1.4 — 2026-07-29

国内模型下载与首次引导稳定性修复。

- X-ASR 默认优先从 ModelScope 国内镜像下载：项目方镜像失败后尝试社区备份，
  最后回退官方 GitHub 整包；所有镜像固定 revision 并逐文件校验 SHA256。
- 模型下载支持 HTTP Range 断点续传；网络中断会保留 `.part`，切换备用源后继续下载。
- 修复 ModelScope LFS CDN 因下载器缺少 User-Agent 返回 403 的问题；公开模型无需用户
  Token，并增加真实匿名下载回归测试。
- 修复首次引导第三步「重新录入」底层键盘钩子失活后只会超时的问题：录入与重新注册
  会主动刷新 LL Hook，窗口内按键也能完成兜底录入。
- 修复首次引导第四步按住说话后偶发收不到松开事件、悬浮窗一直停留且无法发送的问题；
  主键、修饰键、窗口失焦与隐藏路径都会保证按下/松开成对。
- 再次修正深色安装完成页复选框主题，确保「运行 Kotone」「创建桌面快捷方式」文字可见。
- 将前端角色贴纸、背景与纹理改为高质量 WebP，显著缩小安装包中的静态资源体积；
  安装器生成所需的透明 PNG 原稿仍保留，不会影响安装界面素材。

## 0.1.3 — 2026-07-29

流式识别、热词与更新体验修复。

- 修复对讲机模式松手过快导致句尾一两个字尚未完成转录便被发送的问题。
- 独奏模式发送完成后立即开始下一轮收音，不再被短暂的「已发送」状态打断。
- X-ASR 流式引擎的悬浮窗现在显示实时转录结果，不再只显示声波。
- 修复首次引导聊天框获得焦点时全局热键偶发无响应的问题。
- 修复 X-ASR 动态热词的短语分隔、中文 BPE 编码和候选路径配置；用真实录音验证
  「悠米」「璐璐」「残影」「无尽」等低频同音词，并移除每句话重复输出的热词日志。
- 内置 Profile 与热词迁移为独立 JSON 资源；用户自定义词和主动删除记录继续保留。
- 关于页始终显示「检查更新」，最新版本也不会隐藏按钮；启动自动检查仍然保留。
- 添加 MIT License，更新 README 与当前界面截图。
- 修复深色安装完成页中「运行 Kotone」「创建桌面快捷方式」显示为黑色的问题。

## 0.1.2 — 2026-07-29

Windows 用户反馈批次修复。

- 空转录不再发送、不记历史、不再模拟回车——什么都没识别到时无事发生。
- 修复热键偶发「第一次按没反应」：发送成功/提示驻留期内按下热键现在会直接开始下一句。
- 悬浮窗整面可拖动（修复光标显示可拖但只有边框能拖的问题）。
- 悬浮窗按引擎能力区分显示：流式引擎直接显示转录文字，非流式引擎显示声波。
- 悬浮窗长文本改为头部省略，始终显示最新内容；预览气泡自动滚到底部。
- 内置英雄联盟热词从 6 个扩充到 100 个；版本更新新增的内置热词会自动并入老用户配置，
  用户自定义词条不丢失、用户主动删掉的内置词条不会复活。
- VAD 语音判停组件（约 630KB）打包进应用本体，随启动自动就绪；高级页不再展示 VAD 下载项。
- 高级设置页隐藏联调用的 mock 流式引擎。
- 历史记录中保存的音频可直接播放，带声波动画。
- 关于页改版为琴音角色展示页（立绘、性格、母题、贴纸画廊），版本号改为动态读取。
- 标题栏：运行中显示 CPU/内存占用；移除无效的最大化按钮；修复最小化无响应；
  关闭时弹「是否彻底退出」确认（可最小化到托盘）。
- 托盘：左键点击唤出主窗口；右键菜单改为「打开主页面 / 启动或停止引擎 / 退出」，
  移除「显示悬浮条」。
- 非管理员启动时提示可以管理员权限重启，支持「不再提示」与「默认以管理员启动」。
- 接入自动更新：启动后自动检测新版本并提示，确认后下载安装并重启生效。
- 设置页与首次启动向导完成角色化改版，接入新一轮琴音立绘、贴纸、海报、道具与应用图标资产。
- 新增角色档案、退出确认、统一运行按钮，以及诊断信息导出、进程日志和对应自动化测试。
- 安装器改为零成本的自定义 NSIS/MUI2 方案，支持选择安装目录，并使用琴音品牌侧栏与横幅。
- 英雄联盟卡片改用本机游戏资源中的官方图标；README 界面图全部替换为当前版本的真实软件截图。

## 0.1.1 — 2026-07-27

- 修复焦点位于 Kotone 窗口内时热键不触发的问题，并保持按住说话的按下/松开语义。
- 修复录入 Tab、Space、Enter 等热键时触发网页默认行为、导致焦点跳转或按钮误触的问题。
- 为安装器和卸载器应用 Kotone 图标，新增角色霓虹侧栏、品牌横幅与简体中文安装界面。
- 增加“Tab 热键不移动窗口内焦点”的自动化回归测试。

## 0.1.0 — 2026-07-27

Kotone 的首个 Windows 公开版本。

- 完成首次启动向导：选择英雄联盟/通用配置、下载本地识别模型、设置热键并真实发送测试。
- 默认使用 X-ASR 流式中英标点模型，识别在本机完成。
- 支持全局热键、英雄联盟术语热词、通用文字发送与历史记录。
- 悬浮窗支持卡片/胶囊样式、固定位置、拖动记忆和鼠标点击穿透。
- 增加模型缺失、下载失败、热键注册失败与权限不匹配的恢复路径。
- 禁用桌面 WebView 的右键菜单、页面查找、刷新和开发者工具快捷键。
- 设置页按常用功能与高级故障排查重新分层，减少首次使用的配置负担。
